import React from 'react';

const FloatingElements = () => {
  return (
    <div className="floating-elements">
      {/* All floating animations removed */}
    </div>
  );
};

export default FloatingElements;
